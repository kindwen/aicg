# 第二阶段技术实施方案 · 汽车AI内容生产工作台

> 目标：保留现有工作台，修复车型数据/选择逻辑，重构 Prompt 生成架构，接入 AI 策划能力。
> 原则：**数据库管事实 · 规则管约束 · 模板管结构 · AI 管创意**。分阶段落地，不推翻重做。
> 状态：待确认方案，暂不改代码。

---

## 0. 现状代码地图（已审查，作为改造基线）

| 模块 | 关键函数/变量 | 位置（行号） | 现状 |
|---|---|---|---|
| 数据 Provider | `VehicleDataProvider` / `MockProvider` / `UserProvider` / `DcdStaticProvider` / `DongchediProvider` | 805~891 | 4 个 Provider 合并成 `getAll()` |
| 全量数据解码 | `dcdFullDecode` | 782 | **bodyType 硬编码 `'suv'`、energy 取空** |
| 品牌首字母 | `DCD_BRAND_PY`(内联 JSON) / `brandLetter()` / `cmpAZ()` | 771/1363/1387 | A-Z 已实现，`_blCache` 缓存 |
| 榜单排序 | `rankVehicles()` / `rankPos()` / `DCD_RANK` / `dcdRankLoad()` | 1394~1466 | hot/rank 依赖后端，离线回落 `count`+`brandHot` |
| 筛选 | `filterVehicles()` / `renderChips()` / `chipClick` | 1334~1361/2647 | energy/bodyType 来源脏 |
| 车型库页 | `libBrandIndex()` / `renderBrandPicker()` / `azJump()` / `pickBrand()` / `renderLibrary()` | 1643~1763 | A-Z 面板已存在 |
| 内容分类 | `CATS[]`（16 类）+ `tpl()` | 1015~1064 | **tpl 是最终 Prompt 骨架** |
| 标题 | `genTitle(style)` | 1089 | 固定句式 + 车型名 |
| Prompt | `buildPrompt()` / `specCard()` / `compRule()` | 1208/1186/1196 | 纯模板拼接 |
| 状态 | `state`（含 `selected[]` / `catId` / `energyF` / `bodyF` / `visual` 等） | 1258 | 单一 `state` 对象 + `saveState`/`loadState` |
| 资讯/简报 | `BRIEF` / `genBrief()` / `plan15Table()` / `useTopic()` | 1990~2268 | 已完整可用，可复用 |
| 生成动作 | `doGenerate()` / `addToBank()` | 2475/1917 | 存题库 + 历史 |

---

## 1. 数据结构怎么改（车型事实层）

### 1.1 车型对象标准化（`normalizeVehicle`）

新增一个**标准化函数**，所有 Provider 产出都经过它，统一车型对象的字段契约。字段分两类：

**A. 已有但需清洗的字段**

| 字段 | 现状问题 | 修复 |
|---|---|---|
| `bodyType` | DCD 全库硬编码 `'suv'` | 由 `level`（级别）字段映射 + 车名关键词兜底 |
| `energy` | DCD 全库多为空/`—` | 新增 `normalizeEnergy()`，统一为「燃油/纯电/插混/增程/油混/其他」 |
| `id` | `dcs_`/`dcd_`/mock id 三套前缀 | 保留，但统一 `source` 字段标识来源 |
| `verified` | DCD 全库 false | 保留，作为「⚠️待确认」标记依据 |
| `priceMin` | 依赖 `parseMinPrice` | 保留，作为排序/价格带依据 |

**B. 新增的统一事实字段（为空则留 `null`，禁止 AI 补全）**

```
vehicleType   // 轿车/SUV/MPV/跑车/微型车/皮卡/旅行车/硬派越野/其他（由 bodyType 归一）
energy        // 燃油/纯电/插混/增程/油混/其他
range         // 续航（km，无则 null）
powerKw / torqueNm / accelSec
batteryKwh
driveType     // 两驱/四驱 等
dims / wheelbase / seats / trunk
adas          // 智驾能力（无则 null）
tags          // [..]
competitors   // [竞品 seriesId]（暂无数据则 []）
status        // 在售/停产/未上市/概念
source        // '懂车帝车型库' | '内置示例库' | '用户输入'
dataDate      // '2026-09-01'
```

### 1.2 `bodyType` 修复映射

`level` 字段（懂车帝级别，如「中型轿车」「紧凑型SUV」「MPV」）是 bodyType 的最可靠来源：

```js
function inferBodyType(level, name){
  var L=(level||'')+name;
  if(/MPV/.test(L)) return 'mpv';
  if(/跑车|轿跑|Coupe|Roadster/i.test(L)) return 'coupe';
  if(/皮卡|Pickup/i.test(L)) return 'pickup';
  if(/旅行|Wagon|Avant|Shooting/i.test(L)) return 'wagon';
  if(/SUV/.test(L)) return 'suv';
  if(/越野/.test(L)) return 'offroad';
  if(/微型|代步|MINI/i.test(L)) return 'micro';
  if(/轿车|三厢|两厢|Hatchback/i.test(L)) return 'sedan';
  return 'other';
}
```

> `T_SHORT` 扩展为完整映射（补 `coupe/pickup/wagon/mpv/other`），`carSVG` 的 `P`/`W`/`top` 字典同步补车身形态。

### 1.3 `energy` 归一

```js
function normalizeEnergy(raw, name){
  var s=(raw||'')+name;
  if(/纯电|EV|纯电动/i.test(s)) return '纯电';
  if(/插混|插电|PHEV/i.test(s)) return '插混';
  if(/增程|EREV|增程式/i.test(s)) return '增程';
  if(/油混|双擎|HEV|混动/i.test(s)) return '油混';
  if(/燃油|汽油|柴油|T|L自然吸气/i.test(s)) return '燃油';
  return raw || '其他';
}
```

### 1.4 车型索引缓存（性能）

新增 `VehicleIndex` 单例，首次构建后缓存，筛选走索引：

```js
var VehicleIndex = {
  _built:false, all:[], byBrand:{}, byLetter:{}, byEnergy:{}, byBodyType:{}, byRank:{},
  build(){ /* 遍历一次 getAll() 建所有索引 */ },
  invalidate(){ this._built=false; }
};
```

`filterVehicles()` / `libFiltered()` 改为从索引取，避免每次点击 chip 都遍历 4534 条。

---

## 2. state 怎么改（状态统一）

### 2.1 现有 `state` 保留，只做最小扩展

现状已经是单一 `state` 对象 + `saveState/loadState`，**符合「单一 selectedVehicles」诉求**——现有 `state.selected`（数组存 id）就是唯一的车型状态，各模块已通过 `selVehicles()` 读取。**无需重命名，只需补字段**：

```js
state = {
  // —— 已有，保留 ——
  tab, catId, energyF, bodyF, vSearch, rankTab, rankLimit,
  selected, refs, uploads, refOpen, refPhotos, openBrands,
  infoPoints, customNote, title, visual, overridePrompt,
  lib:{...}, libOpen, libPickerOpen, bankF:{...},

  // —— 新增 ——
  goal:'attract',          // 创作目的：attract/news/edu/decision/professional/controversy/planting
  selectedVehiclesOrder: [], // 已选车型的排序（用于 PK 主副车顺序）
  plan:null,               // 最近一次 ContentPlan
  aiPhase:'',              // AI 进度文案：'idle'|'planning'|'title'|'visual'|'prompt'
  visualHistory:[],        // 最近视觉偏好（去黑底）
  titleHistory:[]          // 最近标题（去重负面上下文）
};
```

### 2.2 已选车型顺序

`selected` 仍是 id 数组，顺序即用户选择顺序。新增 `selectedVehiclesOrder` 仅在「调序」UI 操作时显式修改；默认等价于 `selected`。PK 类用 `selected[0]` 作主车、`selected[1]` 作副车。

---

## 3. selectedVehicles 怎么设计

- **单一真相源**：`state.selected`（id 数组，长度 1~4，受 `curCat().min/max` 约束）。
- **读取**：`selVehicles()` 已存在，全项目唯一入口，不动。
- **写入**：`toggleVehicle(id)` 已存在，统一所有「选择/移除/开始创作/用此选题」都走它。
- **同步**：`toggleVehicle` 之后统一调 `refreshWorkbench()`（已存在，刷新 VGrid/Selected/InfoPanel）。
- **新增「已选车型栏」**：在车型工作区顶部渲染 `state.selected` 的缩略卡，支持 `×` 移除 + 左右调序。

---

## 4. VehicleContext 怎么设计

新增纯函数，把 `selected` 组装成给 AI 的事实层（**只读，不缓存到 state**）：

```js
function buildVehicleContext(){
  var vs = selVehicles();
  return {
    vehicles: vs.map(vToFact),          // 每台车完整事实对象
    selectedCount: vs.length,
    primaryVehicle: vToFact(vs[0]),     // 主车
    comparisonVehicles: vs.slice(1).map(vToFact),
    mode: vs.length===1?'single':vs.length===2?'duel':'multi',
    source: 统一来源标记,
    dataDate: 最新数据日期,
    referenceImages: vs.map(v=>({id:v.id, hasUpload:!!state.uploads[v.id], view:state.refs[v.id]}))
  };
}
```

`vToFact(v)` 把标准化后的车型对象映射成 AI 可读事实，**空字段显式 `null`**，并带 `source`/`dataDate`。

---

## 5. ContentPlan 怎么设计

AI 策划层输出的统一结构（前端分块展示，而非一大段 Prompt）：

```js
ContentPlan = {
  coreAngle,          // 核心传播点
  keyMessage,         // 一句话主张
  title,              // 主标题
  alternativeTitles,  // [备选标题 x2]
  recommendedInfoPoints, // 推荐信息点 id 列表
  visualConcept,      // 视觉概念一句话
  scene,              // 场景（白天城市/地库/展厅/山路/家庭…）
  composition,        // 构图（单车居中/双车对峙/田字格）
  camera,             // 视角
  lighting,           // 光线（自然光/棚拍/霓虹…）
  textHierarchy,      // 文字层级说明
  graphicElements,    // 图形元素
  warnings            // [注意事项/⚠️待确认项]
};
```

---

## 6. AI 接口怎么设计

后端 `dongchedi_server.py` 新增两个接口，AI Key 走环境变量，前端不碰：

```
POST /api/ai/plan      body: { vehicleContext, categoryContext, goal, infoPoints, visualSettings, titleHistory, visualHistory }
                       → { plan: ContentPlan }

POST /api/ai/prompt    body: { vehicleContext, categoryContext, plan, visualSettings }
                       → { prompt: string }
```

- 后端从 `os.environ['AI_API_KEY']` + `AI_BASE_URL` + `AI_MODEL` 读配置（`provider/model` 可配）。
- 统一超时（如 30s）、错误捕获，返回结构化 `{ok:false,error}`。
- **前端 AI 封装**：新增 `AiClient.plan()/prompt()`，内部 `fetch('/api/ai/...')`，失败 throw。
- **降级入口**：`AiClient` 封装在 `generateWithAI()` 里，try/catch 失败后自动 `fallbackToTemplate()`（走现有 `buildPrompt`）。

> 一期可先做 `POST /api/ai/generate`（plan+prompt 一次返回，但字段分开），二期再拆两个。方案文档按拆开设计，实现时二选一。

---

## 7. 前端哪些函数修改

| 函数 | 动作 |
|---|---|
| `dcdFullDecode` / `DCD.toVehicle` | 改：接 `inferBodyType`/`normalizeEnergy`，去掉硬编码 |
| 新增 `normalizeVehicle` | 新增：统一字段契约（所有 Provider 出口过一遍） |
| 新增 `inferBodyType`/`normalizeEnergy` | 新增 |
| `T_SHORT` / `carSVG` 字典 | 改：补全车身形态 |
| 新增 `VehicleIndex` | 新增：索引缓存 |
| `filterVehicles`/`libFiltered` | 改：走索引 |
| `rankVehicles`/`dcdRankLoad` | 改：离线时明确「本地数据」标记，不伪造排名 |
| 新增 `buildVehicleContext`/`vToFact` | 新增 |
| 新增 `buildCategoryContext` | 新增：把 CATS + goal + infoPoints + visual 组装成规则层 |
| `genTitle` | 改：从「模板句式」降级为「策略兜底」，AI 在线时被 `AiClient` 覆盖 |
| `buildPrompt` | **保留不动**（作为 Fallback），新增 `buildPromptFromPlan(plan)` 作为 AI 通道 |
| 新增 `generateWithAI` / `AiClient` | 新增 |
| `doGenerate` | 改：先走 AI（含阶段进度文案），失败回退模板 |
| 新增 `renderSelectedBar` | 新增：已选车型栏（调序/移除） |
| `vcardHTML`/`vproHTML` | 改：加「开始创作」按钮 + 「✓已选择」态 |
| `renderChips`/`chipClick` | 改：energy/bodyType 用归一值 |
| 资讯 `useTopic` | 改：打通车型选择（已部分实现，补 `toggleVehicle` 同步） |

---

## 8. 后端哪些接口新增

| 接口 | 说明 |
|---|---|
| `POST /api/ai/plan` | AI 内容策划（返回 ContentPlan） |
| `POST /api/ai/prompt` | 按 ContentPlan 生成最终 Prompt |
| （可选一期）`POST /api/ai/generate` | plan+prompt 合并 |
| `GET /api/rank` | **已有，增强**：返回时附带 `is_live:true/false`，让前端区分实时/降级 |

后端还要：
- 新增 `AI_CONFIG` 环境变量读取（`AI_API_KEY`/`AI_BASE_URL`/`AI_MODEL`）。
- 新增 `ai_plan()`/`ai_prompt()` 函数（urllib 调 LLM，参考现有 `fetch` 封装）。
- 新增 LLM 调用超时/重试/错误返回。

---

## 9. 哪些现有代码保留

| 保留 | 理由 |
|---|---|
| `buildPrompt()` 全套 | AI 失败时的 Fallback，保证工作台可用 |
| `specCard`/`infoValue`/`ipName`/`activeInfoPoints` | 事实提取 + 信息点，仍是规则层核心 |
| `CATS[]`（16 类） | 保留为「内容策略模板」，`tpl()` 降级为结构骨架 |
| `genTitle` | 降级为标题策略兜底（信息型/冲突型/问题型） |
| `INFO_POINTS` / `VIS` / `VIS_DESC` | 保留，作为规则约束 |
| `BRIEF`/`genBrief`/`renderBrief`/`plan15Table`/`useTopic` | 资讯简报整体复用 |
| `DCD` 数据源 / `imgproxy` / `get_rank` / `get_hotwords` | 后端数据能力复用 |
| `state` + `saveState`/`loadState` | 状态持久化机制复用 |
| 品牌 A-Z（`brandLetter`/`renderBrandPicker`/`azJump`） | 已实现，保留并微调 |
| `carSVG`（SVG 车型图） | 保留，作为无图兜底 |

---

## 10. 哪些现有代码废弃/降级

| 项 | 处置 |
|---|---|
| `CATS[].tpl()` 作为「最终 Prompt 模板」的定位 | **降级**为「结构骨架」（AI 负责具体怎么讲），函数本体保留 |
| `genTitle` 的固定句式 | **降级**为兜底；AI 在线时生成标题 |
| `DCD_RANK` 离线伪造排名（`brandHot` 排序伪装成榜单） | **废弃**，改为明确「本地数据/需连接服务」标记 |
| `bodyType`/`energy` 硬编码 | **废弃**，改 `inferBodyType`/`normalizeEnergy` |
| 重复遍历 `allVehicles()` 的筛选 | **废弃**，改索引缓存 |

---

## 11. 如何保证 AI 失败后继续用旧 Prompt 系统

```
doGenerate()
  └─ 若 AI 可用：
        try { plan = await AiClient.plan(...); prompt = await AiClient.prompt(...) }
        catch(e) { fallbackToTemplate() }   // 弹 toast 提示「AI 不可用，已用模板生成」
  └─ 若 AI 不可用（服务未启动/未配 Key）：
        直接 fallbackToTemplate()
fallbackToTemplate() = 现有 buildPrompt() 原样调用
```

- `buildPrompt()` 一行不改，作为永不删除的兜底。
- 前端探测 AI 可用性：`AiClient.ping()`（调后端 `/api/ai/ping` 或直接判 `/api/ai/plan` 是否 404）。
- 任何阶段（plan 失败、prompt 失败）都落到 fallback，UI 只提示「已降级」，不阻断。

---

## 12. 如何保证不破坏当前 UI

- **所有改动基于 `refreshAll`/`refreshWorkbench` 统一入口**，渲染函数间不新增互调。
- 新增组件（已选车型栏、创作目的选择器、AI 进度条、plan 分块展示）**只 append 到现有容器**，不重排现有结构。
- `state` 只增字段，不改已有字段名/类型；`loadState` 对旧 localStorage 数据做兼容（缺新字段用默认值）。
- 标题/Prompt 生成的「自动生成」入口保留，AI 逻辑作为可选增强层叠加，不删除现有按钮。
- 每个阶段完成后用 Playwright 实渲染验证（见 §14），确保无 JS 报错、无布局破坏。

---

## 13. 每个阶段具体修改哪些文件

| 阶段 | 文件 | 内容 |
|---|---|---|
| **一：修车型数据** | `auto-prompt-board.html` | 新增 `normalizeVehicle`/`inferBodyType`/`normalizeEnergy`；改 `dcdFullDecode`/`DCD.toVehicle`；扩展 `T_SHORT`/`carSVG` 字典 |
| **二：修车型选择** | `auto-prompt-board.html` | 新增 `VehicleIndex`；改 `filterVehicles`/`libFiltered`/`rankVehicles`/`dcdRankLoad`；加「本地数据」标记；新增已选车型栏 `renderSelectedBar`；卡片加「开始创作」；资讯 `useTopic` 打通 |
| **三：重构 Prompt 数据结构** | `auto-prompt-board.html` | 新增 `buildVehicleContext`/`vToFact`/`buildCategoryContext`；`state` 加 `goal/plan/selectedVehiclesOrder`；新增创作目的选择器 UI |
| **四：接入 AI（后端）** | `dongchedi_server.py` | 新增 AI 配置读取、`ai_plan`/`ai_prompt`、`POST /api/ai/*` 路由 |
| **五：AI 标题** | `auto-prompt-board.html` | 新增 `AiClient`；`genTitle` 前插入 AI 标题生成 + `titleHistory` 去重 |
| **六：AI Prompt** | `auto-prompt-board.html` | 新增 `buildPromptFromPlan`/`generateWithAI`；改 `doGenerate`（阶段进度 + fallback） |
| **七：历史/去重/视觉多样性** | `auto-prompt-board.html` | `visualHistory`/`titleHistory` 持久化；`buildVehicleContext` 注入历史；「换一个」按钮（局部重生成） |
| **八：资讯与车型打通** | `auto-prompt-board.html` | `useTopic`/`bringNews` 统一走 `toggleVehicle` 同步 |

> 新增代码统一加注释头 `/* [AI-PLAN v2] */`，便于回滚定位。

---

## 14. 每一步完成后如何测试

| 阶段 | 测试方法 |
|---|---|
| 一 | 打开 HTML，F12 无报错；车型库筛选「轿车/SUV/MPV/皮卡」能正确分组；动力筛选「纯电/插混/增程/燃油」能筛出对应车；`bodyType` 不再全是 SUV |
| 二 | 点热门/销量/A-Z 顺序**肉眼可见不同**；服务未启动时榜单区显示「本地数据」提示；已选车型栏能增删调序；「开始创作」一键带入 |
| 三 | F12 打印 `buildVehicleContext()`，字段齐全、空值 null、source/dataDate 正确 |
| 四 | `curl -X POST localhost:8765/api/ai/plan -d '{...}'` 返回合法 ContentPlan；无 Key 时返回 `{ok:false}` 而非 500 |
| 五 | 同车型+同方向连续生成标题**不重复**、不雷同开头 |
| 六 | 点「AI生成」出现分阶段进度文案；断网/停服务时自动回退 `buildPrompt`，工作台不崩 |
| 七 | 连续生成不再全是黑底夜景；「换一个标题」只改标题不重算车型 |
| 八 | 简报「用此选题」→ 车型区同步选中对应车 |

**回归基准**：每个阶段结束跑一次 Playwright 实渲染（打开 `auto-prompt-board.html`），确认：无 console 报错、四个 Tab 可切换、车型库/榜单/资讯/题库正常。

---

## 附：优先级与风险提示

- **阶段一、二是本次用户痛点的直接修复**（榜单无变化、动力/车型默认丰田、A-Z 直达、bodyType/energy 脏数据），应优先落地。
- **阶段三~七是 AI 能力**，依赖后端 LLM 接入，需用户确认 Key/模型来源后再开工。
- **最大风险点**：`DCD_FULL_DATA`（内联 2.2MB 数组）里 `level` 字段并非 100% 覆盖，`inferBodyType` 对「无 level」的车系需靠车名正则兜底，可能仍有少量归为 `other`——这是可接受的降级，不影响主流程。

---

> 本方案为「待确认」状态。确认后按阶段一 → 阶段二 顺序开始编码。
