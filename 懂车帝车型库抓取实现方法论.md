# 懂车帝车型库 + 参考图抓取 · 实现方法论

> 适用对象：任何 AI 编码助手或后端开发者。本文档自包含，不需要额外上下文即可实现。
> 目标：从懂车帝拿到**全量车系库**（品牌 + 车系 + 价格 + 封面）和每车系的**多视角参考图**，供内容创作工具调用。
> 本方法论的结论均经过真实接口实测验证（2026-08）。

---

## 一、目标与验收标准

| 目标 | 验收线 |
|---|---|
| 全量车型库 | ≥ 4000 车系、≥ 400 品牌（实测 4206 车系 / 496 品牌） |
| 搜索能力 | 搜索「特斯拉」能命中 Model Y（即车系名 + 品牌名双匹配） |
| 参考图 | 每车系最多 6 张：封面 1 + 外观 3 + 内饰 2，缺图自动跳过 |
| 图片规格 | 所有 CDN 图统一 `~tplv-resize:750:0`（宽 750px） |
| 性能 | 二次请求命中缓存，不重复抓取 |

---

## 二、关键事实（实测验证，必须先读）

### 2.1 为什么不能直接分页

网页端公开接口：

```
POST https://www.dongchedi.com/motor/brand/m/v6/select/series/?city_name=北京
```

**只返回 Top100 热门车系，`offset` 分页参数无效** —— 无论 offset 传多少都返回同一批 100 个。这是全量抓取的核心障碍。

### 2.2 突破口：App 域名接口支持按品牌过滤

```
POST https://www.dcdapp.com/motor/brand/m/v1/select/series/?city_name=北京
Content-Type: application/json
Body: {"offset":0, "limit":200, "is_refresh":1, "city_name":"北京", "brand": <brand_id>}
```

特性：
- 支持 `brand` 参数按品牌过滤，返回**该品牌全部车系**（不受 Top100 限制）
- `limit` 可拉到 200
- **`brand_id` 是连续整数**：遍历 `1~40` 即可覆盖全部有效品牌（实测 38 个有效）

### 2.3 配套接口

| 用途 | 接口 | 说明 |
|---|---|---|
| 详情 | `GET https://www.dongchedi.com/motor/car_page/m/v1/series_all_json/?series_id={sid}&city_name=北京&show_city_price=1` | 返回 `series_name`、`brand_name`、`cover_url`、车型分组（online/urban/offline） |
| 图库 | `GET https://www.dongchedi.com/motor/pc/car/series/get_series_picture?aid=1839&app_name=auto_web_pc&series_id={sid}&category={cat}&offset=0&count=1` | `cat=wg` 外观、`cat=ns` 内饰；返回 `picture_list[0].large_pic_url`（数组，**无角度标签**） |
| 请求头 | 所有请求带 `User-Agent: Mozilla/5.0` + `Referer: https://www.dongchedi.com/` | 模拟浏览器，避免被拒 |

---

## 三、实现步骤

### Step 1 · 品牌遍历构建全量库

```
series_map = {}        # 按 series_id 去重
valid_brands = []      # 记录有车系的品牌 id

for bid in range(1, 41):
    POST App接口, body={"offset":0,"limit":200,"is_refresh":1,"city_name":"北京","brand":bid}
    解析 data.series 数组
    if 空 → 跳过（无效品牌）
    else:
        valid_brands.append(bid)
        for s in series:
            sid = s.id 或 s.concern_id
            if sid 已存在 → 跳过（去重）
            series_map[sid] = {
                id, name(s.outter_name), brand_id, sub_brand_id,
                price(s.dealer_price), min_price, max_price,
                cover(规范化后的 cover_url), count
            }
    sleep(0.05~0.2)   # 防限流
```

### Step 2 · 品牌名补全

车系数据里只有 `brand_id` 没有品牌名。对每个有效品牌，取该品牌**第一个车系**调详情接口：

```
for bid in valid_brands:
    sid = 该品牌任一车系的 id
    detail = get_detail(sid)
    brands[bid] = detail.brand_name
    sleep(0.05)
```

得到 `brand_id → brand_name` 映射。

### Step 3 · 持久化 + 缓存

```
写盘: {"built_at": 时间戳, "brands": {...}, "series": [...]}
读逻辑: 内存缓存 → 磁盘缓存(7天内有效) → 重新构建
```

缓存 7 天自动过期重建，避免频繁全量遍历。

### Step 4 · 参考图抓取

```
每车系:
  封面  = 详情接口 cover_url                                    (1张)
  外观  = 图库接口 category=wg → picture_list[0].large_pic_url 取前3张
  内饰  = 图库接口 category=ns → picture_list[0].large_pic_url 取前2张
  结果按车系缓存（内存 dict，key=series_id）
```

### Step 5 · 对外 API

本地起一个 HTTP 服务（推荐 Python 标准库，零依赖），提供：

| 路由 | 功能 |
|---|---|
| `/api/brands` | 品牌列表（从全量库聚合） |
| `/api/series-by-brand?brand_id=` | 按品牌取车系 |
| `/api/series?keyword=` | 本地全量搜索（**车系名 + 品牌名双匹配**） |
| `/api/detail?id=` | 车系详情 + 车型分组 |
| `/api/photos?id=` | 多视角参考图（cover/exterior/interior） |
| `/imgproxy?u=` | 图片转发代理（解决浏览器跨域） |

### Step 6 · 前端对接

- 搜索框 → `/api/series?keyword=`
- 品牌列表 → `/api/brands`，点选 → `/api/series-by-brand`
- 选中车系 → `/api/detail?id=` + `/api/photos?id=`，参考图 `<img src="/imgproxy?u=...">`

---

## 四、已知坑与规避（重要）

| 坑 | 现象 | 处理 |
|---|---|---|
| offset 分页无效 | 翻页永远是同一批 Top100 | 放弃分页，改按 brand 遍历 |
| 图片代理 502 | 正则把 `.jpg` 后缀吞掉 | URL 规范化时必须**保留扩展名**再拼接 CDN 参数 |
| 车型分组脏数据 | `car_group_list_key` 含 `NoneT`/`None马力`/`None` | 清洗后从车名正则提取「续航 km / N座 / 四驱」等关键词兜底 |
| 搜品牌搜不到 | 搜「特斯拉」无结果（车系名不含"特斯拉"） | 搜索逻辑同时匹配车系名 + 品牌名 |
| 浏览器跨域 | 直接请求字节 CDN 被 CORS 拦 | 本地 `/imgproxy` 服务端转发 |
| 价格随城市变 | 不同城市经销商报价不同 | 固定 `city_name=北京` |
| 品牌 ID 会变 | 新增品牌/ID 漂移 | 扫描范围可配置（默认 40），缓存到期自动重建 |
| 限流 | 连续请求被拒 | 遍历间隔 0.05~0.2s，失败静默跳过 |

---

## 五、核心代码骨架（Python 标准库）

```python
import json, time, re, urllib.request, urllib.parse

CITY = "北京"
CITY_ENC = urllib.parse.quote(CITY)
HEADERS = {"User-Agent": "Mozilla/5.0", "Referer": "https://www.dongchedi.com/"}

DCDAPP_SERIES = "https://www.dcdapp.com/motor/brand/m/v1/select/series/?city_name=" + CITY_ENC
DETAIL_URL = ("https://www.dongchedi.com/motor/car_page/m/v1/series_all_json/"
              "?series_id={sid}&city_name=" + CITY_ENC + "&show_city_price=1")
PICTURE_URL = ("https://www.dongchedi.com/motor/pc/car/series/get_series_picture"
               "?aid=1839&app_name=auto_web_pc&series_id={sid}&category={cat}&offset=0&count=1")

def fetch(url, method="GET", data=None, timeout=12):
    body = json.dumps(data).encode() if data is not None else None
    req = urllib.request.Request(url, data=body, headers=HEADERS, method=method)
    return urllib.request.urlopen(req, timeout=timeout).read().decode("utf-8")

def norm_img(url):
    """图片URL规范化: 统一 750px 宽, 保留扩展名"""
    if not url:
        return ""
    if url.startswith("//"):
        url = "https:" + url
    m = re.search(r"~tplv-[^?]*?\.(jpg|jpeg|png|webp)", url)
    if m:
        url = re.sub(r"~tplv-[^?]*?\.(jpg|jpeg|png|webp)",
                     "~tplv-resize:750:0." + m.group(1), url)
    return url

def build_full_series(scan_range=40):
    """Step1+2: 遍历品牌构建全量库"""
    series_map, valid_brands = {}, []
    for bid in range(1, scan_range + 1):
        try:
            obj = json.loads(fetch(DCDAPP_SERIES, "POST",
                                   {"offset": 0, "limit": 200, "is_refresh": 1,
                                    "city_name": CITY, "brand": bid}))
            items = (obj.get("data") or {}).get("series") or []
        except Exception:
            items = []
        if items:
            valid_brands.append(bid)
            for s in items:
                sid = str(s.get("id") or s.get("concern_id") or "")
                if not sid or sid in series_map:
                    continue
                series_map[sid] = {
                    "id": sid, "name": s.get("outter_name") or "",
                    "brand_id": s.get("brand_id") or bid,
                    "price": s.get("dealer_price") or "",
                    "min_price": s.get("min_price"), "max_price": s.get("max_price"),
                    "cover": norm_img(s.get("cover_url") or ""),
                }
        time.sleep(0.1)
    brands = {}
    for bid in valid_brands:
        sid = next((s["id"] for s in series_map.values() if s["brand_id"] == bid), None)
        if not sid:
            continue
        try:
            d = json.loads(fetch(DETAIL_URL.format(sid=sid)))
            name = (d.get("data") or {}).get("brand_name") or ""
            if name:
                brands[bid] = name
        except Exception:
            pass
        time.sleep(0.05)
    return {"built_at": time.time(), "brands": brands, "series": list(series_map.values())}

def get_photos(sid):
    """Step4: 封面+外观x3+内饰x2"""
    result = {"cover": "", "exterior": [], "interior": []}
    try:
        d = json.loads(fetch(DETAIL_URL.format(sid=sid)))
        result["cover"] = norm_img((d.get("data") or {}).get("cover_url") or "")
    except Exception:
        pass
    for cat, key, n in (("wg", "exterior", 3), ("ns", "interior", 2)):
        try:
            d = json.loads(fetch(PICTURE_URL.format(sid=sid, cat=cat)))
            pl = ((d.get("data") or {}).get("picture_list") or [])
            urls = (pl[0].get("large_pic_url") or pl[0].get("pic_url") or []) if pl else []
            for u in urls[:n]:
                uu = norm_img(u)
                if uu and uu not in result[key]:
                    result[key].append(uu)
        except Exception:
            pass
        time.sleep(0.15)
    return result
```

---

## 六、搜索实现要点

```
query 小写化
对全量 series 逐条判断:
    series.name 包含 query      → 命中
    brands[series.brand_id] 包含 query → 命中
返回命中列表（车系名在前、可加 count 热度排序）
```

---

## 七、交付检查清单

- [ ] 全量库 ≥ 4000 车系 / 400 品牌
- [ ] 缓存 7 天有效，冷启动才重建
- [ ] 搜索同时匹配车系名 + 品牌名
- [ ] 图片 URL 统一 750px 且保留扩展名
- [ ] 参考图封面 1 + 外观 3 + 内饰 2，缺图跳过
- [ ] 对外接口全部走本地代理（图片转发 /imgproxy）
- [ ] 请求间隔 ≥ 0.05s，失败静默不中断
